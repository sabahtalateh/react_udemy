export enum BooksActions {
    REQUESTED = 'BOOKS_REQUESTED',
    LOADED = 'BOOKS_LOADED',
    ERROR = 'BOOKS_ERROR',
}

// const booksLoaded = (books: BookProps[]) => ({
//     type: BOOKS_LOADED,
//     payload: books
// })
// export { booksLoaded }
